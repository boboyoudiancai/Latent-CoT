Checkpoint directories in this repository contain lightweight metadata only:

- `config.yaml`
- `config.json`
- `dataset_statistics.json`
- run commands and small summaries when available

Large checkpoint weights such as `.pt`, `.pth`, `.safetensors`, `.bin`, and
similar binary artifacts are intentionally excluded from Git.
