This directory intentionally excludes LIBERO/LeRobot datasets.

On the source machine, `data/libero_lerobot` was a symlink to:

`/home/liuyue/starVLA/playground/Datasets/libero_cot`

Datasets are not stored in this Git repository.
